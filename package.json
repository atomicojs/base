{
  "name": "atomico-base",
  "description": "a repo to start with atomico",
  "version": "0.0.0",
  "scripts": {
    "start": "bundle src/components/**/*.md docs --watch --server --template template/template.html",
    "build:docs": "bundle src/components/**/*.md docs --minify",
    "build:components": "bundle src/components/**/*-*.js ./ --minify"
  },
  "dependencies": {
    "atomico": "^0.20.0"
  },
  "devDependencies": {
    "bundle-cli": "^0.19.1",
    "tfolder-cli": "0.0.0"
  },
  "exports": {
    ".": "./dest/"
  }
}
